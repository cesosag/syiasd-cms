import colors from './colors';
import fontWeights from './fontWeights';
import sizes from './sizes';
import Logo from '../assets/images/SY-logo.svg';

const theme = {
  main: {
    colors,
    fontWeights,
    sizes,
    logo: Logo,
  },
};

export default theme;
